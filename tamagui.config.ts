import { createTamagui } from 'tamagui' // ← import de 'tamagui'
import { config } from '@tamagui/config/v2' // ← tema padrão

const tamaguiConfig = createTamagui(config)

export default tamaguiConfig